
# Rename Dropdown Title to "How We Work"

## Change
Update the button label in `src/components/ProgramsDropdown.tsx` from **"Programs & Services"** to **"How We Work"**.

## Technical Detail
- **File**: `src/components/ProgramsDropdown.tsx`, line ~30
- Change the text inside the `<button>` element from `Programs & Services` to `How We Work`
- No other files need updating — this is the single source of the label.
